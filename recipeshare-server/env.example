# Server environment variables for local development and Render

# Render provides PORT automatically. Locally you can keep 5000.
PORT=5000

# Set a strong secret in production
JWT_SECRET=change_me_in_prod

# MongoDB connection string. Use mongodb+srv for Atlas.
# Example: mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
MONGO_URI=

# Frontend origin for CORS (dev and prod). In Render, set to your Static Site URL.
CORS_ORIGIN=http://localhost:5173

# Public URL where this API is reachable (used to build absolute image URLs)
# Will automatically use RENDER_EXTERNAL_URL on Render if not set.
PUBLIC_URL=http://localhost:5000

# Optional overrides (defaults exist). If using a Render disk, set UPLOAD_DIR to the mounted path.
# UPLOAD_DIR=
# DATA_FILE=
